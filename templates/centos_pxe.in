# this is generated by pxebuilder.
default CentOS_KS_Install
prompt 1
timeout 5
#display boot.msg
LABEL CentOS_KS_Install
    MENU LABEL KS_Install
    kernel ${centos_path}/vmlinuz
    APPEND initrd=${centos_path}/initrd.img ramdisk_size=300000 ks=nfs:${nfs_ip}:${nfs_ans}/${answerfile} ksdevice=${host_nic} text
