# this is generated by pxebuilder.
accepteula
rootpw ${host_pass}
clearpart --firstdisk --overwritevmfs
install --firstdisk  --overwritevmfs
network --bootproto=static --ip=${host_ipaddr} --gateway=${host_gate} --nameserver=${host_ns} --netmask=${host_mask} --hostname=${hostname} --device=${host_nic} 
reboot --noeject

%firstboot --interpreter=busybox
# Enable HV (Hardware Virtualization to run nested 64bit Guests + Hyper-V VM)
#grep -i "vhv.allow" /etc/vmware/config || echo "vhv.allow = \"TRUE\"" >> /etc/vmware/config
 
# Enable & start remote ESXi Shell (SSH)
vim-cmd hostsvc/enable_ssh
vim-cmd hostsvc/start_ssh
 
# Enable & start ESXi Shell (TSM)
vim-cmd hostsvc/enable_esx_shell
vim-cmd hostsvc/start_esx_shell