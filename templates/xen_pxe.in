# this is generated by pxebuilder.
DEFAULT default
LABEL default
    KERNEL ${xen_path}/mboot.c32
    APPEND ${xen_path}/xen.gz dom0_max_vcpus=2 dom0_mem=752M com1=115200,8n1 console=com1,vga tty --- ${xen_path}/vmlinuz xencons=hvc console=hvc0 console=tty0 answerfile=nfs://${nfs_ip}:${nfs_ans}/${answerfile} answerfile_device=${xen_nic} install --- ${xen_path}/install.img
