# this is generated by pxebuilder.
DEFAULT default
LABEL default
    KERNEL ${http_server}/boot/mboot.c32
    APPEND ${http_server}/${url_path}/boot/xen.gz dom0_max_vcpus=2 dom0_mem=752M com1=115200,8n1 console=com1,vga tty --- ${http_server}/${url_path}/boot/vmlinuz xencons=hvc console=hvc0 console=tty0 answerfile=${http_server}/${kick_url}/${answerfile} answerfile_device=${xen_nic} install --- ${http_server}/${url_path}/install.img
